"""MCP servers for snowtuner.

Currently ships one server: the admin MCP (``snowtuner.mcp.admin``), which
exposes the snowtuner FastAPI as MCP tools so a data platform admin can
ask Claude Desktop questions about optimizer state.
"""
